import { photoType } from './photoType';
import { siteSettingsType } from './siteSettingsType';
import { tagType } from './tagType';

export const schemaTypes = [photoType, tagType, siteSettingsType];
